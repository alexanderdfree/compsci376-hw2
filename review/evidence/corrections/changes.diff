--- before/ANSWERS.md
+++ after/ANSWERS.md
@@ -2,16 +2,19 @@
 
 COMPSCI 376 / LINGUIST 399 — code-examples exercise. The instructor's five Brown-corpus
 scripts (Modules → Code Examples) are kept verbatim in `originals/`; each has an
-adaptation in the repo root that reads its corpus from `corpus_config.py`. Every number
-and sentence quoted below comes from `output/`, regenerated by `./run_all.sh` with random
-seed 376, so the results are reproducible.
+adaptation in the repo root that reads its corpus from `corpus_config.py`. N-gram frequency
+tables, generated examples, and comparison statistics come from `output/`, regenerated by
+`./run_all.sh` with random seed 376. Corpus inventories and additional counts were checked
+directly against the installed data. These results use Python 3.12.14 and NLTK 3.10.3;
+exact reproduction also depends on using the same corpus and tokenizer data. Lengths below
+count **tokens, including punctuation**, unless otherwise specified.
 
 ## 1. Which corpus did you choose?
 
 NLTK's **`inaugural`** corpus: the 60 US presidential inaugural addresses from Washington
-(1789) through Biden (2021).
-
-| corpus | words | sentences | files |
+(1789) through Trump's second inauguration (2025).
+
+| corpus | tokens | sentences | files |
 |---|---:|---:|---:|
 | **inaugural** (chosen) | 156,288 | 5,395 | 60 |
 | gutenberg | 2,621,613 | 98,552 | 18 |
@@ -24,21 +27,24 @@
 | genesis | 315,268 | 13,644 | 8 |
 | treebank | 100,676 | 3,914 | 199 |
 
-I picked it because it is sentence-segmented (`.sents()` works, which the language-model
-code requires), its formal register makes it easy to judge whether a generated sentence
-"sounds like a sentence", and it is small enough that every experiment runs in about two
-seconds. One wrinkle: the post-2009 addresses are stored as UTF-8 but NLTK decodes them as
-Latin-1, so curly quotes and em-dashes become junk tokens (`\x80\x94`, `¡`) and an `â`
-gets glued onto the preceding word. That shows up in question 3.
+I picked it because it supports sentence tokenization through `.sents()`, which the
+language-model code requires, its formal register makes generated sentences easy to compare,
+and it is small enough to run the experiments locally. The `punkt_tab` tokenizer data is
+needed in addition to the corpus; `corpus_config.py` downloads both resources.
+
+One wrinkle: the post-2009 addresses are stored as UTF-8 but this NLTK corpus reader decodes
+them as Latin-1, so curly quotes and em-dashes become junk tokens such as `\x80\x94`, and
+an `â` gets glued onto the preceding word. Older text also contains encoding artifacts
+such as `¡`. That shows up in question 3.
 
 ## 2. Most frequent unigram, bigram, and trigram
 
 Counts from `ngram_freq.py` (punctuation kept, as in `UnigramBigramTrigramFreq.py`) and
-`ngram_freq_nopunct.py` (punctuation removed with `string.punctuation`, as in
+`ngram_freq_nopunct.py` (the `word not in string.punctuation` filter, as in
 `UnigramBigramTrigramNoPuncFreq.py`). `bigram_frequencies.py` agrees: `of the` is the top
 bigram among 68,149 distinct bigrams.
 
-| | punctuation kept | punctuation removed |
+| | punctuation kept | punctuation filter applied |
 |---|---|---|
 | unigram | **the** (9,670) — then `,` 7,499, `of` 7,260 | **the** (9,670) |
 | bigram | **of the** (1,784) — then `, and` 1,404 | **of the** (1,784) |
@@ -46,7 +52,7 @@
 
 Top ten of each, side by side:
 
-| rank | unigram (kept) | bigram (kept) | trigram (kept) | unigram (no punct) | bigram (no punct) | trigram (no punct) |
+| rank | unigram (kept) | bigram (kept) | trigram (kept) | unigram (filtered) | bigram (filtered) | trigram (filtered) |
 |---:|---|---|---|---|---|---|
 | 1 | the 9,670 | of the 1,784 | . It is 155 | the 9,670 | of the 1,784 | the United States 154 |
 | 2 | , 7,499 | , and 1,404 | the United States 154 | of 7,260 | in the 765 | of the United 101 |
@@ -59,23 +65,28 @@
 | 9 | our 2,126 | , the 388 | . We are 68 | be 1,545 | of a 266 | as well as 47 |
 | 10 | that 1,807 | . It 354 | of the world 66 | is 1,506 | the people 259 | of the country 47 |
 
-Two things worth noticing. With punctuation kept, the comma is the second most common
-"word", three of the top ten bigrams and five of the top ten trigrams contain punctuation,
-and the top trigram `. It is` is really a sentence-boundary statistic: "It is" is the most
-common way an inaugural sentence begins. Removing punctuation also manufactures adjacencies
-that never occurred: `to the` rises from 716 to 718 because tokens that were separated by a
-comma or dash become neighbours, and `. The` / `. We` disappear entirely. The
-`string.punctuation` filter only matches single characters, so `--` (363), `."` (74),
-`,"` (23), `...` (5) and the mojibake tokens (`\x80\x94` 46, `¡` 18) all survive it;
-14,329 tokens (9%) were removed.
+With punctuation kept, the comma is the second most common token, and **five** of the top
+ten bigrams and five of the top ten trigrams contain punctuation. The frequency scripts
+operate on a flat token stream, so n-grams can cross sentence boundaries: the top trigram
+`. It is` reflects that adjacency. Checking sentence openings directly, `It is` is also
+the most frequent initial bigram, occurring 158 times.
+
+Filtering punctuation creates additional adjacencies: `to the` rises from 716 to 718
+because two occurrences of `to " the` lose their quotation mark. `. The` and `. We`
+disappear entirely. Membership in `string.punctuation` is a **substring test**, not a
+general test for whether every character is punctuation. Tokens such as `--` (363),
+`."` (74), `,"` (23), `...` (5), and the mojibake tokens (`\x80\x94` 46, `¡` 18)
+survive it. The filter removed 14,329 tokens (9.2%); all removed tokens in this corpus
+happened to be single characters.
 
 ## 3. Did punctuation tokens impact sentence generation? How?
 
 Yes, in four distinct ways.
 
 **Bad starting points.** The random-start script picks a starting bigram uniformly from all
-67,961 distinct contexts, and 8,841 of them (13%) contain punctuation, so sentences begin
-mid-clause or even on a comma. From `output/basic_lm_random_start.txt` and
+68,068 distinct contexts, and 8,187 of them (12.0%) contain a token with no letters or
+digits, excluding the `None` padding. Sentences can therefore begin mid-clause or even on
+a comma. From `output/basic_lm_random_start.txt` and
 `output/improved_frequent_start.txt`:
 
 > of magnanimity , one home , take it .
@@ -84,78 +95,101 @@
 > , as to retain persons known to all .
 > , invading neither the narrow confines of a similar character , situation , is needed to secure …
 
-"relationship ." and "exposition belongs ." happen when the chosen bigram is
-`(word, ".")`: the only thing the model has ever seen after a period is the end-of-sentence
-padding, so the sentence stops immediately. Over 500 generated sentences, only 9% (uniform
-start) and 6% (frequency-weighted start) began with a capitalised word.
-
-**Good terminators.** Because `.` is an ordinary token that is always followed by the `None`
-padding, the model has effectively learned "period means stop": 99-100% of generated
-sentences in every punctuation-kept configuration end with terminal punctuation. Stripping
-punctuation (`--punct strip`) removes that stopping cue and every comma, and the output turns
-into run-ons with no internal structure (mean 26.2 words, 0/500 ending with terminal
-punctuation, and one 124-word run-on in `output/improved_boundary_nopunct.txt`).
-
-**Comma chains.** Kept commas are the glue that lets a trigram model keep going with only
-two words of memory: the longest sentence in `output/basic_lm.txt` is 84 words, and the
-longer a sentence gets the further it drifts from whatever it started saying.
+"relationship ." starts from `('relationship', '.')`, which is followed by end padding.
+"exposition belongs ." instead starts from `('exposition', 'belongs')` and generates the
+period. In the rewritten generator's 500-sentence experiments, 8.6% of uniform-start outputs
+and 5.8% of frequency-weighted-start outputs begin with a capitalised token. These rates
+describe the cleaned model used in question 4, not the untouched baseline.
+
+**Sentence endings.** A period is usually followed by `None` padding, but there are
+exceptions: 37 of the corpus's 5,186 period tokens occur inside sentences, including
+`Mr . Jefferson` and `St . Croix`. Generation ultimately stops on the `None` marker.
+Across the punctuation-kept configurations in question 4, 98.8–100.0% of outputs end
+with terminal punctuation.
+
+Stripping punctuation (`--punct strip`) removes visible sentence and clause markers,
+but the model still learns and samples `None` at sentence ends. The resulting text is
+harder to read without those written cues: the mean is 26.2 tokens, 0/500 outputs end
+with terminal punctuation, and one unpunctuated output has 124 tokens
+(`output/improved_boundary_nopunct.txt`).
+
+**Comma chains.** The generated text can chain clauses and drift from its opening topic.
+The longest sentence in `output/basic_lm.txt` is 84 tokens, including punctuation. Two
+tokens of context are not enough to maintain a coherent argument through a long output.
 
 **Junk tokens.** The mojibake survives the punctuation filter and gets generated:
 
 > … and we will seize itâ  so long as the woe due to the present have turned their thoughts toward home …
 
-(`output/basic_lm.txt`, the `â` is the first byte of a mangled em-dash). `improved_lm.py`
-strips non-ASCII bytes from every token before training, which removes these.
+(`output/basic_lm.txt`, the `â` comes from misdecoding the first byte of an em-dash).
+`improved_lm.py` strips non-ASCII characters from every token before training. This removes
+the displayed artifacts, but is a lossy cleanup that can also remove legitimate characters.
 
 ## 4. Were you able to generate reasonable sentences? What did you try?
 
-Running the instructor's random-start code unchanged on `inaugural` mostly produced
-fragments (see question 3). I then rewrote it as `improved_lm.py`, keeping the same
-count-then-sample model but exposing each tweak as a flag, and measured every configuration
-on 500 sentences (`output/comparison.txt`):
-
-| configuration | mean words | verbatim corpus copies | starts with capital | ends with . ! ? |
+The adaptation of the instructor's random-start generator produced many fragments
+(see question 3). I then rewrote it as `improved_lm.py`, keeping the count-then-sample
+approach but exposing each tweak as a flag. The table compares configurations of this
+**rewritten generator**, each run on 500 sentences (`output/comparison.txt`). Its uniform
+start uses the original start-selection strategy, but the rewrite also changes preprocessing,
+sampling, stopping, and the hard cap, so this row is not the original baseline.
+
+All rows strip non-ASCII characters. The cleaned trigram model with punctuation kept has
+67,961 contexts, compared with 68,068 in the raw baseline. “Verbatim copies” means exact
+matches to corpus sentences after the same cleaning and punctuation processing as the model.
+Terminal punctuation includes tokens such as `."` and `...`, as well as `.`, `!`, and `?`.
+
+| configuration | mean tokens | verbatim corpus copies | starts with capital | ends with terminal punctuation |
 |---|---:|---:|---:|---:|
-| original: uniform random starting bigram | 25.9 | 0% | 9% | 99% |
-| start from a frequent bigram (frequency-weighted) | 26.2 | 0% | 6% | 99% |
-| start from the sentence boundary | 27.4 | 2% | 99% | 99% |
-| boundary start, punctuation stripped | 26.2 | 2% | 100% | 0% |
-| boundary start, bigram model (1 word of context) | 28.5 | 0% | 100% | 100% |
-| boundary start, 4-gram model (3 words of context) | 26.1 | 18% | 100% | 99% |
-| **boundary start, trigram, 6-30 words, reject verbatim** | **15.9** | **0%** | **99%** | **100%** |
+| rewritten trigram, uniform random starting bigram | 25.9 | 0/500 (0.0%) | 8.6% | 98.8% |
+| start from a frequent bigram (frequency-weighted) | 26.2 | 0/500 (0.0%) | 5.8% | 99.0% |
+| start from the sentence boundary | 27.4 | 11/500 (2.2%) | 99.4% | 98.8% |
+| boundary start, punctuation stripped | 26.2 | 8/500 (1.6%) | 99.6% | 0.0% |
+| boundary start, bigram model (1 token of context) | 28.5 | 2/500 (0.4%) | 99.8% | 99.6% |
+| boundary start, 4-gram model (3 tokens of context) | 26.1 | 89/500 (17.8%) | 100.0% | 99.0% |
+| **boundary start, trigram, 6-30 tokens, reject verbatim** | **15.9** | **0/500 (0.0%)** | **99.2%** | **99.6%** |
+| boundary start, bigram, 6-30 tokens, reject verbatim | 16.3 | 0/500 (0.0%) | 99.8% | 99.4% |
 
 What each tweak did:
 
 1. **Start at the sentence boundary** (`--start boundary`). The model already contains the
-   context `(None, None)`, whose continuations are exactly the words that open real
-   sentences (The, We, It, I, …). Sampling the first two words from there instead of picking
+   context `(None, None)`, whose continuations are the tokens that open real
+   sentences (The, We, It, I, quotation marks, parentheses, …). Sampling the first two tokens
+   from there instead of picking
    a random bigram is the direct fix for the hint in the assignment: `of the` is the most
    frequent bigram in the corpus, but not one of the 5,395 sentences begins with lowercase `of the` (four begin with
    "Of the"), so it is a terrible starting point. The most frequent *sentence-initial* bigrams are `It is`,
-   `We have`, `We will`, and boundary sampling picks them in proportion. Capitalised starts
-   went from 9% to 99%. Weighting the random start by bigram frequency (`--start frequent`)
-   made things worse (6%), which confirms that frequency and "good starting point" are
-   different properties.
+   `We have`, `We will`, and boundary sampling picks them in proportion. In the rewritten
+   trigram model, capitalised starts went from 8.6% with uniform starts to 99.4% with
+   boundary starts. Weighting random starts by bigram frequency (`--start frequent`)
+   lowered this measure to 5.8%, illustrating that frequency and sentence-initial suitability
+   are different properties. Capitalisation alone does not measure sentence quality.
 2. **Cleaner sampling.** The accumulate-until-`r` loop became `random.choices` weighted by
-   raw counts (same distribution, no normalisation pass, no floating-point edge case), with a
-   hard length cap so a sentence can never loop forever.
+   raw counts, representing the same empirical transition probabilities for a fixed
+   preprocessed corpus. A hard cap bounds the generation loop. This does not reproduce
+   the baseline's exact seeded sequence, which also consumes different boundary draws.
 3. **N-gram order** (`--context K`). I kept the assignment's bigram context (a trigram
-   model) as the default and compared it with one and three words of context. The bigram
-   model never copies the corpus but wanders, because it forgets everything but the last word:
+   model) as the default and compared it with one and three tokens of context. The bigram
+   model copied 2/500 corpus sentences (0.4%): `Thank you .` and `Amen .`. It also wanders,
+   because it remembers only the previous token:
    > The snow was to foster capital , the independence , rather might such high place to act , and record numbers .
 
-   The 4-gram model reads beautifully but 18% of its output (8 of the 20 in
-   `output/improved_4gram_model.txt`) is copied verbatim, for example Nixon's 1969
+   The 4-gram model produces some fluent examples but copies 89/500 sentences (17.8%).
+   In the smaller 20-sentence sample (`output/improved_4gram_model.txt`), 8/20 are copies,
+   for example Nixon's 1969
    "We see tasks that need doing , waiting for hands to do them ." With 124,020 distinct
-   three-word contexts in a 156k-word corpus, most contexts have only one possible
-   continuation, so the model has memorised the speeches. The trigram model is the sweet
-   spot at 2% copies.
-4. **Length filter and novelty check** (`--min-len 6 --max-len 30 --novel`). A trigram model
-   drifts in proportion to length, so rejecting anything under 6 or over 30 words (about 40%
-   of draws) and anything that appears verbatim in the corpus roughly halves the mean length
-   and makes most survivors coherent.
-5. **Punctuation stripping** was tried and rejected (question 3): keeping punctuation and
-   simply never *starting* on it, which boundary starts guarantee, works better.
+   three-token contexts, 113,863 (91.8%) have only one possible continuation, which helps
+   explain why generation often follows memorised passages. I kept the trigram model as a
+   compromise; its unfiltered boundary-start run copied 11/500 sentences (2.2%).
+4. **Length filter and novelty check** (`--min-len 6 --max-len 30 --novel`). The filter
+   accepts 6–30 tokens, including punctuation, and rejects exact cleaned-corpus sentence
+   matches. Producing 500 accepted sentences took 867 draws: 358 were rejected for length
+   and nine for verbatim copying. The mean dropped from 27.4 to 15.9 tokens, but neither
+   the length filter nor the novelty check guarantees grammaticality or coherence.
+5. **Punctuation stripping** was tried and rejected (question 3): I preferred keeping the
+   written punctuation and sampling observed sentence openings. Boundary starts greatly
+   reduce arbitrary mid-sentence openings, but can still start with punctuation: 25 training
+   sentences do, and four of the final 500 outputs start with quotes or parentheses.
 
 Final configuration:
 
@@ -163,7 +197,7 @@
 python improved_lm.py --start boundary --context 2 --min-len 6 --max-len 30 --novel --seed 376
 ```
 
-Sample output (`output/improved_final.txt`, none of these sentences occur in the corpus):
+Sample output (`output/improved_final.txt`; none exactly match a cleaned corpus sentence):
 
 > It is time rather to speak .
 > These basic precepts are not at fault .
@@ -180,12 +214,13 @@
 > Look , folks , all the markets of the advisers of my countrymen are just and advantageous to both purposes .
 > We need all our earthly hopes are entwined .
 
-The first welds Biden's 2021 "Look, folks" onto nineteenth-century diction, and the second is
-fine at every three-word window but ungrammatical as a whole. Two words of context carry no
-notion of topic, era, register, or long-range agreement, which is the fundamental ceiling of
-a count-based n-gram model, and the reason the next step up is smoothing and interpolation
-across orders, a much larger corpus (`--corpus state_union` or `gutenberg` work out of the
-box), or a neural language model.
-
-So: yes, with boundary starts, a trigram model, and a length/novelty filter, most generated
-sentences are reasonable short inaugural-sounding sentences; long ones still drift.
+The first combines Biden's 2021 "Look, folks" with older formal diction, and the second
+uses observed trigrams but is ungrammatical as a whole. Two tokens of context provide no
+explicit representation of topic, era, or long-range agreement. Further experiments could
+try smoothing or interpolation across orders, or a larger corpus (`--corpus state_union`
+or `gutenberg`), although those changes alone do not give this model longer-range memory.
+
+These changes produced some plausible short inaugural-sounding sentences, while others
+remain fragments or incoherent combinations. The measurements show better sentence openings,
+shorter outputs, and fewer exact corpus copies; they do not establish that most outputs
+are coherent.
--- before/CLAUDE.md
+++ after/CLAUDE.md
@@ -8,11 +8,12 @@
 
 ## Environment and commands
 
-Python lives in the `compsci376` conda env (miniforge), shared by the whole course repo (`../requirements.txt`). NLTK data is in `~/nltk_data`; `corpus_config.py` downloads whatever corpus it needs on import, so a fresh machine only needs the package install. Run scripts from inside this folder so the sibling `corpus_config` import resolves.
+Python lives in the `compsci376` conda env (miniforge), shared by the whole course repo (`../requirements.txt`). NLTK data is in `~/nltk_data`; `corpus_config.py` ensures both the selected corpus and `punkt_tab` sentence-tokenizer data are downloaded. A fresh machine needs the Python package and these data resources; the first script run can download the resources automatically. Run scripts from inside this folder so the sibling `corpus_config` import resolves.
 
 ```bash
 conda activate compsci376                # or use ~/miniforge3/envs/compsci376/bin/python directly
 pip install -r ../requirements.txt
+python -m nltk.downloader inaugural punkt_tab  # explicit setup; also done by corpus_config
 
 ./run_all.sh                             # regenerate every file in output/ (seed 376, ~20 s)
 python basic_lm.py 376                   # one script; the optional arg is the random seed
@@ -36,12 +37,15 @@
   | `Basic LM with Sentence Generation with Random Starting Bigram.py` | `basic_lm_random_start.py` |
 
 - `corpus_config.py` is the single switch for which corpus every adapted script uses (`CORPUS_NAME`); scripts import `corpus` from it rather than `nltk.corpus.<name>` directly.
-- All generators share one model shape: `model[(w1, w2)][w3]` = count, built from `trigrams(sent, pad_left=True, pad_right=True)`, so `None` marks sentence boundaries and generation stops when `None` is sampled. Despite the file name, the "starting bigram" is the two-word *context* of a trigram LM, not a bigram model. `improved_lm.py` generalises this to `--context K` and adds start strategy, punctuation stripping, length filtering, and a verbatim-copy check.
+- The baseline generators count `model[(w1, w2)][w3]` with `trigrams(sent, pad_left=True, pad_right=True)` and then normalize the counts into probabilities. `None` marks sentence boundaries; the baselines stop after two trailing `None` values, while the rewrite stops at the first sampled `None`. Despite the file name, the "starting bigram" is the two-token *context* of a trigram LM, not a bigram model. `improved_lm.py` generalises this to `--context K` and adds start strategy, punctuation stripping, length filtering, and an exact cleaned-corpus sentence-copy check.
 - `output/` is fully generated by `run_all.sh`; `ANSWERS.md` quotes from it, so rerun the script before changing the answers. `output/comparison.txt` is the 500-sentence statistics table behind question 4.
+- The comparison table contains configurations of the rewritten generator, including its uniform-start configuration. It is not a measurement of the untouched baseline. Lengths count tokens, including punctuation; percentages use one decimal place and include raw counts.
+- The checked environment is Python 3.12.14 / NLTK 3.10.3, with 60 inaugural addresses from 1789 through 2025. Reproducing exact outputs also requires the same corpus and tokenizer data; a random seed alone does not pin those resources.
 
 ## Gotchas
 
-- The `inaugural` corpus's post-2009 files are read by NLTK as Latin-1, producing junk tokens (`\x80\x94`, `¡`, a stray `â` glued to words). `improved_lm.py` strips non-ASCII bytes; the faithful adaptations deliberately do not.
-- `string.punctuation` (used by the NoPunc original) only matches single characters, so `--`, `."`, `...` survive its filter. `ngram_freq_nopunct.py` reports those survivors.
+- The `inaugural` corpus's post-2009 files are read by NLTK as Latin-1, producing junk tokens (`\x80\x94`, a stray `â` glued to words). Older text also contains encoding artifacts such as `¡`. `improved_lm.py` strips non-ASCII characters; the faithful adaptations deliberately do not.
+- `word in string.punctuation` (used by the NoPunc original) tests whether the token is a substring of that string. Tokens such as `--`, `."`, and `...` survive its filter. `ngram_freq_nopunct.py` reports those survivors.
+- Periods usually precede the sentence-end padding, but abbreviations such as `Mr . Jefferson` do not. Boundary starts sample observed openings, which can include quotes or parentheses; they do not guarantee a punctuation-free start. Stripping punctuation retains the `None` stopping marker.
 - The original `Basic LM` prints `dict(model["part","of","the"])`, a 3-tuple key on a 2-tuple-keyed dict, so it always prints `{}`; `basic_lm.py` prints the intended `("of", "the")` distribution instead.
 - The original generators loop forever if a starting context is absent from the model; keep starting words that occur in the chosen corpus.
--- before/corpus_config.py
+++ after/corpus_config.py
@@ -7,11 +7,14 @@
 """
 import nltk
 
-CORPUS_NAME = "inaugural"  # US presidential inaugural addresses, 1789-2021
+CORPUS_NAME = "inaugural"  # Installed corpus: inaugural addresses, 1789-2025
 
 
 def load_corpus(name=CORPUS_NAME):
-    nltk.download(name, quiet=True)
+    # Plaintext corpus readers need sentence-tokenizer data as well as the corpus.
+    # Installing the nltk Python package alone does not install these resources.
+    nltk.download(name, quiet=True, raise_on_error=True)
+    nltk.download("punkt_tab", quiet=True, raise_on_error=True)
     return getattr(nltk.corpus, name)
 
 
--- before/improved_lm.py
+++ after/improved_lm.py
@@ -5,26 +5,28 @@
 so most outputs begin mid-phrase ("of magnanimity , one home , take it .").
 This script keeps the same counting/sampling idea but makes the knobs explicit:
 
-  --context K      words of context (1 = bigram LM, 2 = trigram LM as in the
+  --context K      tokens of context (1 = bigram LM, 2 = trigram LM as in the
                    original, 3 = 4-gram LM)
   --start HOW      boundary  start from the None padding, so the first words are
                              drawn from the sentence-initial distribution (default)
-                   uniform   the original behaviour: any context, equally likely
-                   frequent  a real-word context chosen in proportion to its
+                   uniform   any context, equally likely, in the cleaned model
+                   frequent  a non-padding context chosen in proportion to its
                              corpus frequency (demonstrates why "frequent" is not
                              the same as "good starting point")
   --punct MODE     keep (default) or strip punctuation tokens before training
-  --min-len/--max-len   reject and regenerate sentences outside this word range
-  --novel          reject sentences that appear verbatim in the corpus
+  --min-len/--max-len   reject sentences outside this token range, including punctuation
+  --novel          reject exact matches to cleaned corpus sentences
   --quiet          print only the summary statistics
 
-Non-ASCII bytes are always stripped from tokens: the post-2009 inaugural files
+Non-ASCII characters are always stripped from tokens: the post-2009 inaugural files
 are decoded as Latin-1 by NLTK, which turns curly quotes and em-dashes into
 junk tokens such as '\\x80\\x94' and glues an 'â' onto the preceding word.
+Uniform starts use the original start-selection strategy, but preprocessing,
+sampling, stopping, and the hard cap differ from the baseline implementation.
 
 Examples:
   python improved_lm.py --seed 376
-  python improved_lm.py --start uniform --seed 376        # reproduce the original
+  python improved_lm.py --start uniform --seed 376        # compare start strategies
   python improved_lm.py --context 1 --punct strip -n 10
   python improved_lm.py --min-len 6 --max-len 30 --novel -n 500 --quiet
 """
@@ -97,12 +99,12 @@
 def main():
     ap = argparse.ArgumentParser(description=__doc__, formatter_class=argparse.RawDescriptionHelpFormatter)
     ap.add_argument("--corpus", default=CORPUS_NAME)
-    ap.add_argument("--context", type=int, default=2, help="words of context (default 2 = trigram LM)")
+    ap.add_argument("--context", type=int, default=2, help="tokens of context (default 2 = trigram LM)")
     ap.add_argument("--start", choices=["boundary", "uniform", "frequent"], default="boundary")
     ap.add_argument("--punct", choices=["keep", "strip"], default="keep")
-    ap.add_argument("--min-len", type=int, default=1)
-    ap.add_argument("--max-len", type=int, default=0, help="0 = unlimited")
-    ap.add_argument("--novel", action="store_true", help="reject verbatim corpus sentences")
+    ap.add_argument("--min-len", type=int, default=1, help="minimum tokens, including punctuation")
+    ap.add_argument("--max-len", type=int, default=0, help="maximum tokens, including punctuation; 0 = no upper-length filter")
+    ap.add_argument("--novel", action="store_true", help="reject exact matches to cleaned corpus sentences")
     ap.add_argument("-n", type=int, default=20, help="sentences to generate")
     ap.add_argument("--seed", type=int, default=None)
     ap.add_argument("--quiet", action="store_true")
@@ -140,10 +142,10 @@
     capitalized = sum(w[0][:1].isupper() for w in outputs)
     terminal = sum(w[-1] in TERMINAL for w in outputs)
     mean_len = sum(len(w) for w in outputs) / n
-    print(f"--- {len(outputs)} sentences, mean length {mean_len:.1f} words, "
-          f"verbatim copies of corpus sentences {verbatim}/{n} ({100*verbatim/n:.0f}%), "
-          f"start with a capital {capitalized}/{n} ({100*capitalized/n:.0f}%), "
-          f"end with terminal punctuation {terminal}/{n} ({100*terminal/n:.0f}%), "
+    print(f"--- {len(outputs)} sentences, mean length {mean_len:.1f} tokens, "
+          f"verbatim copies of cleaned corpus sentences {verbatim}/{n} ({100*verbatim/n:.1f}%), "
+          f"start with a capital {capitalized}/{n} ({100*capitalized/n:.1f}%), "
+          f"end with terminal punctuation {terminal}/{n} ({100*terminal/n:.1f}%), "
           f"rejected {dict(rejected) or 0}")
 
 
--- before/ngram_freq_nopunct.py
+++ after/ngram_freq_nopunct.py
@@ -1,8 +1,9 @@
-"""Adapted from originals/UnigramBigramTrigramNoPuncFreq.py: top-10 n-grams with
-single-character punctuation removed (the original filters with string.punctuation).
+"""Adapted from originals/UnigramBigramTrigramNoPuncFreq.py: top-10 n-grams using
+the original `word not in string.punctuation` filter.
 
-Also reports which multi-character punctuation tokens (e.g. "--", "''") survive that
-filter, since string.punctuation only matches single characters.
+Membership in a string tests substrings, not individual characters only. Many
+multi-character punctuation tokens (e.g. "--", "''") survive this filter; the
+script reports the remaining tokens with no alphanumeric characters.
 """
 from nltk import bigrams, trigrams, FreqDist
 import string
--- before/output/comparison.txt
+++ after/output/comparison.txt
@@ -1,24 +1,24 @@
 corpus=inaugural sentences=5395 context=2 start=uniform punct=keep min_len=1 max_len=inf novel=False seed=376 contexts=67961
---- 500 sentences, mean length 25.9 words, verbatim copies of corpus sentences 0/500 (0%), start with a capital 43/500 (9%), end with terminal punctuation 494/500 (99%), rejected 0
+--- 500 sentences, mean length 25.9 tokens, verbatim copies of cleaned corpus sentences 0/500 (0.0%), start with a capital 43/500 (8.6%), end with terminal punctuation 494/500 (98.8%), rejected 0
 
 corpus=inaugural sentences=5395 context=2 start=frequent punct=keep min_len=1 max_len=inf novel=False seed=376 contexts=67961
---- 500 sentences, mean length 26.2 words, verbatim copies of corpus sentences 0/500 (0%), start with a capital 29/500 (6%), end with terminal punctuation 495/500 (99%), rejected 0
+--- 500 sentences, mean length 26.2 tokens, verbatim copies of cleaned corpus sentences 0/500 (0.0%), start with a capital 29/500 (5.8%), end with terminal punctuation 495/500 (99.0%), rejected 0
 
 corpus=inaugural sentences=5395 context=2 start=boundary punct=keep min_len=1 max_len=inf novel=False seed=376 contexts=67961
---- 500 sentences, mean length 27.4 words, verbatim copies of corpus sentences 11/500 (2%), start with a capital 497/500 (99%), end with terminal punctuation 494/500 (99%), rejected 0
+--- 500 sentences, mean length 27.4 tokens, verbatim copies of cleaned corpus sentences 11/500 (2.2%), start with a capital 497/500 (99.4%), end with terminal punctuation 494/500 (98.8%), rejected 0
 
 corpus=inaugural sentences=5392 context=2 start=boundary punct=strip min_len=1 max_len=inf novel=False seed=376 contexts=68087
---- 500 sentences, mean length 26.2 words, verbatim copies of corpus sentences 8/500 (2%), start with a capital 498/500 (100%), end with terminal punctuation 0/500 (0%), rejected 0
+--- 500 sentences, mean length 26.2 tokens, verbatim copies of cleaned corpus sentences 8/500 (1.6%), start with a capital 498/500 (99.6%), end with terminal punctuation 0/500 (0.0%), rejected 0
 
 corpus=inaugural sentences=5395 context=1 start=boundary punct=keep min_len=1 max_len=inf novel=False seed=376 contexts=10139
---- 500 sentences, mean length 28.5 words, verbatim copies of corpus sentences 2/500 (0%), start with a capital 499/500 (100%), end with terminal punctuation 498/500 (100%), rejected 0
+--- 500 sentences, mean length 28.5 tokens, verbatim copies of cleaned corpus sentences 2/500 (0.4%), start with a capital 499/500 (99.8%), end with terminal punctuation 498/500 (99.6%), rejected 0
 
 corpus=inaugural sentences=5395 context=3 start=boundary punct=keep min_len=1 max_len=inf novel=False seed=376 contexts=124020
---- 500 sentences, mean length 26.1 words, verbatim copies of corpus sentences 89/500 (18%), start with a capital 500/500 (100%), end with terminal punctuation 495/500 (99%), rejected 0
+--- 500 sentences, mean length 26.1 tokens, verbatim copies of cleaned corpus sentences 89/500 (17.8%), start with a capital 500/500 (100.0%), end with terminal punctuation 495/500 (99.0%), rejected 0
 
 corpus=inaugural sentences=5395 context=2 start=boundary punct=keep min_len=6 max_len=30 novel=True seed=376 contexts=67961
---- 500 sentences, mean length 15.9 words, verbatim copies of corpus sentences 0/500 (0%), start with a capital 496/500 (99%), end with terminal punctuation 498/500 (100%), rejected {'verbatim': 9, 'length': 358}
+--- 500 sentences, mean length 15.9 tokens, verbatim copies of cleaned corpus sentences 0/500 (0.0%), start with a capital 496/500 (99.2%), end with terminal punctuation 498/500 (99.6%), rejected {'verbatim': 9, 'length': 358}
 
 corpus=inaugural sentences=5395 context=1 start=boundary punct=keep min_len=6 max_len=30 novel=True seed=376 contexts=10139
---- 500 sentences, mean length 16.3 words, verbatim copies of corpus sentences 0/500 (0%), start with a capital 499/500 (100%), end with terminal punctuation 497/500 (99%), rejected {'length': 365}
+--- 500 sentences, mean length 16.3 tokens, verbatim copies of cleaned corpus sentences 0/500 (0.0%), start with a capital 499/500 (99.8%), end with terminal punctuation 497/500 (99.4%), rejected {'length': 365}
 
--- before/output/improved_4gram_model.txt
+++ after/output/improved_4gram_model.txt
@@ -19,4 +19,4 @@
 Our children are watching in schools throughout our great land .
 The influence of this force in both instances the most effectual measures in our power have been taken , without interfering with its other duties , for the inheritance which awaits them .
 America , at its best , is also courageous .
---- 20 sentences, mean length 19.8 words, verbatim copies of corpus sentences 8/20 (40%), start with a capital 20/20 (100%), end with terminal punctuation 20/20 (100%), rejected 0
+--- 20 sentences, mean length 19.8 tokens, verbatim copies of cleaned corpus sentences 8/20 (40.0%), start with a capital 20/20 (100.0%), end with terminal punctuation 20/20 (100.0%), rejected 0
--- before/output/improved_bigram_model.txt
+++ after/output/improved_bigram_model.txt
@@ -19,4 +19,4 @@
 One people , but efficiently performs the laws , it relates to the result less favored .
 For we own conscience of civilization has blessed us all of history than in its responsibility -- not provided an interstate commerce , as despotic , when a constructive effort , baby , and the Government itself .
 And let us nor merely transferring power confided to indulge in social order to join with a certain and industrial pursuits , will favor was great blotches of the world , we stand , by the consciousness of the framework of the civil and exact tribute from their liberty animates our nation prospers under a long - year or else , and as circumstances the laws determining factor in our people and its major purpose of it is to compete and sectional , has since have the officers .
---- 20 sentences, mean length 27.1 words, verbatim copies of corpus sentences 0/20 (0%), start with a capital 20/20 (100%), end with terminal punctuation 20/20 (100%), rejected 0
+--- 20 sentences, mean length 27.1 tokens, verbatim copies of cleaned corpus sentences 0/20 (0.0%), start with a capital 20/20 (100.0%), end with terminal punctuation 20/20 (100.0%), rejected 0
--- before/output/improved_boundary_nopunct.txt
+++ after/output/improved_boundary_nopunct.txt
@@ -19,4 +19,4 @@
 We have treated them as time and designed forever to remain so
 We wish to promote peace but we can not conceive that by our power alone cannot protect us nor fearing to approach these problems
 In thousands of prayer
---- 20 sentences, mean length 27.5 words, verbatim copies of corpus sentences 2/20 (10%), start with a capital 20/20 (100%), end with terminal punctuation 0/20 (0%), rejected 0
+--- 20 sentences, mean length 27.5 tokens, verbatim copies of cleaned corpus sentences 2/20 (10.0%), start with a capital 20/20 (100.0%), end with terminal punctuation 0/20 (0.0%), rejected 0
--- before/output/improved_boundary_start.txt
+++ after/output/improved_boundary_start.txt
@@ -19,4 +19,4 @@
 It is the cement of the channel of a thrilling new era in production , transportation and trade resumes its wonted channels .
 God bless you and God bless America .
 When this spirit is suffered to lie long and sometimes difficult .
---- 20 sentences, mean length 24.1 words, verbatim copies of corpus sentences 1/20 (5%), start with a capital 20/20 (100%), end with terminal punctuation 20/20 (100%), rejected 0
+--- 20 sentences, mean length 24.1 tokens, verbatim copies of cleaned corpus sentences 1/20 (5.0%), start with a capital 20/20 (100.0%), end with terminal punctuation 20/20 (100.0%), rejected 0
--- before/output/improved_final.txt
+++ after/output/improved_final.txt
@@ -19,4 +19,4 @@
 This signifies more than any other .
 They adhere to the shores of an intelligent people .
 I may safely give the proper course to renew our hope by a memory .
---- 20 sentences, mean length 13.8 words, verbatim copies of corpus sentences 0/20 (0%), start with a capital 20/20 (100%), end with terminal punctuation 20/20 (100%), rejected {'verbatim': 1, 'length': 17}
+--- 20 sentences, mean length 13.8 tokens, verbatim copies of cleaned corpus sentences 0/20 (0.0%), start with a capital 20/20 (100.0%), end with terminal punctuation 20/20 (100.0%), rejected {'verbatim': 1, 'length': 17}
--- before/output/improved_frequent_start.txt
+++ after/output/improved_frequent_start.txt
@@ -19,4 +19,4 @@
 United States , it is our home industry ," and already are our dreams .
 , invading neither the narrow confines of a similar character , situation , is needed to secure greater elasticity in the history of the United States , but to be reminded that the President the sole heirs of the Government can and must provide for them until they could not live .
 adherence to the establishment of direct lines of attack , and have negotiated many treaties to secure economy and only justice , liberty , we shall attain that high confidence you reposed in me .
---- 20 sentences, mean length 28.5 words, verbatim copies of corpus sentences 0/20 (0%), start with a capital 3/20 (15%), end with terminal punctuation 20/20 (100%), rejected 0
+--- 20 sentences, mean length 28.5 tokens, verbatim copies of cleaned corpus sentences 0/20 (0.0%), start with a capital 3/20 (15.0%), end with terminal punctuation 20/20 (100.0%), rejected 0
--- before/output/improved_uniform_start.txt
+++ after/output/improved_uniform_start.txt
@@ -19,4 +19,4 @@
 States can do this .
 seasons brought .
 no better time to speak , warning the people , still believe that all these blessings , not our yesterdays ."
---- 20 sentences, mean length 19.9 words, verbatim copies of corpus sentences 0/20 (0%), start with a capital 1/20 (5%), end with terminal punctuation 19/20 (95%), rejected 0
+--- 20 sentences, mean length 19.9 tokens, verbatim copies of cleaned corpus sentences 0/20 (0.0%), start with a capital 1/20 (5.0%), end with terminal punctuation 19/20 (95.0%), rejected 0
--- before/run_all.sh
+++ after/run_all.sh
@@ -1,5 +1,5 @@
 #!/usr/bin/env bash
-# Regenerates every file in output/ that ANSWERS.md quotes.  Seeded, so reruns are reproducible.
+# Regenerates output/ with a fixed seed; exact results also depend on NLTK and corpus data.
 # Usage: ./run_all.sh            (uses the compsci376 conda env)
 #        PYTHON=python ./run_all.sh   (inside an already-activated env)
 set -euo pipefail
@@ -23,7 +23,8 @@
 "$PYTHON" improved_lm.py --start boundary --context 3 --seed "$SEED" > output/improved_4gram_model.txt
 "$PYTHON" improved_lm.py --start boundary --min-len 6 --max-len 30 --novel --seed "$SEED" > output/improved_final.txt
 
-# Summary statistics over 500 sentences per configuration.
+# Summary statistics over 500 sentences per rewritten-generator configuration.
+# The uniform-start row uses the cleaned model, not the untouched baseline.
 {
   for cfg in "--start uniform" "--start frequent" "--start boundary" \
              "--start boundary --punct strip" "--start boundary --context 1" \
